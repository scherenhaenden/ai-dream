import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ApiService } from '../core/api.service';
@Component({standalone:true,changeDetection:ChangeDetectionStrategy.OnPush,template:`
<div class="page-head"><div><div class="eyebrow">PREFERENCES</div><h1>Settings</h1><p>Configure how this browser connects to the local AI Dream service.</p></div><span class="page-badge"><i></i>LOCAL</span></div>
<section class="surface settings-card"><div class="section-title"><div><h2>Local API</h2><p>Requests stay on your machine.</p></div><span class="status-tag" [class.ok]="api.connected()">{{ api.connected() ? 'Connected' : api.connection() === 'checking' ? 'Checking' : 'Unavailable' }}</span></div><label class="field-label" for="api-url">Base URL</label><div class="input-row"><input id="api-url" type="url" [value]="url" (input)="url = $any($event.target).value" spellcheck="false" placeholder="http://127.0.0.1:8765" (keydown.enter)="save()"><button class="primary-button" (click)="save()">Save & check</button></div><p class="help">Health check: <code>GET {{ api.baseUrl() }}/api/health</code>. The base URL is stored in this browser's local storage.</p>@if (urlError()) {<p class="error-line" role="alert">{{ urlError() }}</p>} @else if (api.error()) {<p class="error-line">{{ api.error() }}</p>}</section>
<div class="page-note"><span>ⓘ</span><span>Service-specific controls become available as the Python API is wired to each screen.</span></div>
`})
export class SettingsPage { url = this.api.baseUrl(); readonly urlError = signal(''); constructor(readonly api: ApiService) {} save() { if (!this.api.setBaseUrl(this.url)) { this.urlError.set('Use http://127.0.0.1:<port> or http://localhost:<port> with a port from 1 to 65535.'); return; } this.urlError.set(''); } }
