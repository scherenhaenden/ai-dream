import { ChangeDetectionStrategy, Component } from '@angular/core'; import { PageShell } from './page-shell';
@Component({standalone:true,imports:[PageShell],changeDetection:ChangeDetectionStrategy.OnPush,template:`<page-shell title="Downloads" section="SYSTEM" description="Track model transfers to your local model library." endpoint="GET /api/downloads (planned)" symbol="⇩" connectedText="Download tracking is not connected to this web client yet."></page-shell>`}) export class DownloadsPage {}
